
# pandas
# Ett Python-bibliotek för att jobba med tabeller (som Excel).
# En tabell i pandas heter ofta DataFrame

# DataFrame
# En tabell med:
# rader (observations)
# kolumner (variabler)

# pickle / .pkl
# Ett sätt att spara Python-objekt (t.ex. en DataFrame) i en fil så att man kan ladda den snabbt igen.
# Inte text som JSON, utan binärt Python-format.

# NumPy
# Ett bibliotek för snabb matte och arrayer (listor i 2D/3D osv).
# NumPy-arrayer är det scikit-learn (ML-biblioteket) gillar att jobba med.

# SVC / SVM
# SVM = en modell som försöker rita en linje (eller kurva) som skiljer två klasser åt.
# SVC är scikit-learns klass för att träna en SVM-classifier.

# Vad är scikit-learn?
# Ett maskininlärningsbibliotek med färdiga modeller.
# 🔹 SVC(kernel='linear', C=1000)
# ➡️ Skapar en SVM-modell
# kernel='linear' → rakt beslutsgräns
# C → hur hårt modellen försöker klassificera korrekt

